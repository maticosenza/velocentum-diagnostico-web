# BV4 · F1 Foundation — handoff

1. Commit candidato **local** en `feat/bv4-rebranding`, desde `831ef34`. **Sin push**: la rama nunca tocó `origin`, `main` intacto, `MOTOR_DOCUMENTAL_ACTIVO` y `TEMA_DOCUMENTAL_ACTIVO` sin activar nada nuevo.
2. QA: `npm test` **72 archivos / 903 pruebas** verdes con la suite v1 intacta · `tsc --noEmit` exit 0 · `vite build` exit 0 con **los mismos dos warnings de fontkit que el commit base**, ni uno más · `npm run dev` arranca en 324 ms y devuelve **200** · doble corrida de los cuatro generadores con **SHA-256 idéntico**.
3. Tema `velocentum-crystal/v1` al lado de v1 e **inactivo por defecto**: 20 pares de contraste medidos por el propio código, los 20 pasan AA. Sólo **dos hexadecimales derivados** en todo el tema, ambos calculados y documentados; ningún pink vinculante se ajustó.
4. Geist Mono bajada de la **fuente oficial de Vercel** (v1.7.2, SHA-256 del ZIP y de cada archivo registrados), 4 pesos como data URI, licencia commiteada. Los 15 glyphs exigidos, verificados con fontkit en los **29** archivos de fuente del repo, 0 problemas.
5. **17 SVG de marca** al repositorio sin modificar, verificados byte a byte contra la biblioteca oficial. `crystal-v-short-b.svg` no se copió (DH-6/DH-8). Lockup construido y **no aplicado a ninguna superficie**.
6. **Hallazgo — filtros:** los assets con `feGaussianBlur` son **seis, no cinco** (se suma `treatments/solid.svg`), y `@react-pdf/primitives` 4.x **no tiene ninguna primitiva de filtro**: el desenfoque no es representable en PDF. Además react-pdf no resuelve `stroke="url(#gradiente)"` (prisma, barras, isotipo) y **aborta el render** de las 6 pills y de `scroll-axis` por `font-family="Arial"`. Reportado, **nada decidido**.
7. **Hallazgo — defecto de asset:** `objects/prism.svg` trae un `<path>` sin `fill` declarado y el navegador lo rellena de negro (el valor inicial de `fill` en SVG es `black`). Casi seguro no intencional.
8. **Pendiente humano:** veredicto del isotipo (gate DH-6) sobre `docs/bv4-f1-isotipo-test.png`, y aprobación visual sobre `docs/bv4-f1-muestra-visual.html`. Hasta entonces todo uso del isotipo queda **provisional**.
9. **Recomendación de entorno, no defecto del repo:** el repo vive en `~/Documents` y está desalojado a iCloud (`compressed,dataless` en ~98% de `node_modules`). En frío, importar un solo plugin tardó **848 s** y el SSR del `dev` moría por el timeout de 60 s de Vite; materializado el árbol, todo corre normal. E-22 queda **verificado y cerrado**; conviene liberar disco o sacar el repo de `~/Documents`.
10. Artefactos para la auditoría externa en `artefactos/bv4-f1-artefactos-<hash>.zip`, generado desde un **worktree limpio** del commit candidato. **Detención completa: el push llega recién con autorización expresa.**
