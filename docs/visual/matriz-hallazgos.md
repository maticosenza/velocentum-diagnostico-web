# Matriz de hallazgos

Una fila por hallazgo de `docs/visual/auditoria-visual-2026-08-23.md`.
Verificado contra `feat/noche-continuacion`, HEAD
`e5080e20b2be491c3f45ad9846fc07441e68c103`.

**Bloque visual que lo corrige:** 1 = se resuelve/clarifica en este mismo
bloque documental (sin tocar código); 2 = requiere trabajo de
diseño/implementación visual directo; 3 = requiere una decisión de
producto y/o un cambio estructural más profundo (contrato de tipos, motor,
arquitectura de componentes) antes de que un cambio visual tenga sentido.

**Estado de verificación:** exactamente uno de "confirmado en código" /
"no localizado" / "refutado".

## Errores comprobados

| ID | Hallazgo (resumido) | Clase | Archivo y línea de la causa | Capa | Bloque que lo corrige | Estado de verificación |
|---|---|---|---|---|---|---|
| E-01 | Texto/tabla de escenarios solapados en `impresion` y `pantalla` | NORMATIVO | `src/documents/renderers/pdf/document.tsx:97` (`cardWidthGrid`) + `:623-648` (tabla mensual de 5 columnas dentro de una tarjeta de ese ancho) | renderer | 2 | confirmado en código — el ancho de tarjeta (`31.8%`/`48.8%`) y una tabla de 5 columnas conviven en el mismo `card`, sin overflow ni scroll horizontal ni reducción de columnas; el mecanismo del solapamiento a 400 DPI no se re-renderizó en este bloque (documental), pero la causa estructural sí está localizada |
| E-02 | Página con encabezado y cuerpo vacío en escenarios (16:9) | NORMATIVO | `src/documents/templates/velocentum-v1/blocks.ts:160-239` (`buildScenarios`, UN bloque con todos los escenarios, no una sección por escenario) + `src/documents/renderers/pdf/document.tsx:825-856` (`ContentPage`, paginación automática de `@react-pdf/renderer`, header `fixed`) | renderer + contrato | 2 | refutado en parte — la causa presunta original ("una sección por escenario sin verificar contenido") no se sostiene: `buildScenarios` arma UN solo bloque `scenarios` con TODOS los ítems, no hay "una sección por escenario". La causa real más probable es desbordamiento de contenido (mismo origen que E-01) empujando el cuerpo a la página siguiente mientras el header `fixed` se repite — no verificado con un PDF regenerado en este bloque, así que queda como hipótesis fundada, no confirmación plena |
| E-03 | Palancas mezclan tres magnitudes sin etiqueta, muestran ritmo día 90 en vez de acumulado | NORMATIVO | `src/lib/escenarios-90d.ts:156` (`PalancaLinea`, sólo `montoMensualDia90`) + `src/documents/domain/escenarios-90d.ts:120-124` + `src/documents/templates/velocentum-v1/blocks.ts:199-207` (`type` se transporta pero no se usa para mostrar) + `src/documents/renderers/pdf/document.tsx:650-654` y `src/documents/renderers/web/document-renderer.tsx:372-384` (`lever.type` sólo en la `key` de React, nunca en el texto visible) | dominio (valor correcto) + renderer (presentación) | 2 | confirmado en código — ver también V1 más abajo |
| E-04 | "Sin datos — Sin datos" como cifra principal | NORMATIVO | `src/documents/domain/resumen-comercial.ts:115-131` (origen) + `src/documents/renderers/pdf/document.tsx:567-571` (texto hardcodeado) | dominio (retención correcta) + renderer (texto) | 2 | confirmado en código — con el agregado de que el renderer web muestra "Retenido" para el mismo estado, no "Sin datos" (divergencia PDF/web no listada con ID propio, ver `docs/visual/contrato-estados.md` sección 3) |
| E-05 | `no_aplica` mostrado como "dato faltante" | NORMATIVO | `src/documents/domain/build-context.ts:556-563` (`merTienda`) | dominio | 3 | refutado en su formulación literal — no hay ningún `no_aplica` mal mostrado; el valor es genuinamente `retenido` según el tipo. El problema real de fondo (si "inversión $0 declarada" debería ser `no_aplica` en vez de `retenido` para MER) es una decisión de producto no tomada — ver `docs/visual/contrato-estados.md` sección 3 |
| E-06 | Volcado de 19 tarjetas de retención casi idénticas, con concordancia rota | NORMATIVO + INFERIDO | `src/documents/templates/velocentum-v1/blocks.ts:179-190` (`retainedRestriction` una por combinación magnitud × acumulado/ritmo × escenario) — el texto "retenida" está hardcodeado en el template del label, sin concordancia de género | contrato (agregación) + copy | 2 | confirmado en código — 3 magnitudes × 2 (acumulado/ritmo) × 3 escenarios = hasta 18 restricciones individuales posibles, más la del headline = 19; "Ahorro publicitario retenida" es agramatical porque la plantilla de texto (`blocks.ts:182,187`) no varía el adjetivo por género del sustantivo |
| E-07 | Badge de confianza "ALTA" en escenario íntegramente retenido; nota de reinversión sin condición | NORMATIVO | `src/documents/domain/escenarios-90d.ts:190-215`, línea 201 (`confianza: confianzaDocumento`) + `src/documents/renderers/pdf/document.tsx:617-621` y `src/documents/renderers/web/document-renderer.tsx:314-318` (nota sin condicional) | dominio (confianza no se recalcula por escenario) + renderer (nota incondicional) | 3 | confirmado en código |
| E-08 | Duplicación exacta de hallazgos entre diagnóstico y propuesta | NORMATIVO | `src/documents/templates/velocentum-v1/diagnostico.ts:38`, `propuesta.ts:13`, `composicion.ts:19` — las tres llaman `buildFindings(context)` sobre el mismo `context.hallazgos`, sin ningún parámetro de diferenciación | contrato/plantilla | 3 | confirmado en código — ver `docs/visual/inventario-componentes.md` |
| E-09 | Servicio concatenado como ítem propio del catálogo | NORMATIVO | `src/lib/propuesta.ts:216` (`"Desarrollo y optimización web y Meta Ads"` hardcodeado) + `src/documents/domain/build-context.ts:268-281` (dedupe de string crudo, sin partir contra `SERVICIOS`) | contrato | 3 | confirmado en código — ver V2/C-03 más abajo |
| E-10 | Margen negativo sin alerta (ni diagnóstico ni propuesta lo mencionan) | NORMATIVO | Antes de `e5080e2`: sin hallazgo dedicado. Después de `e5080e2` (ya en HEAD): `src/lib/propuesta.ts` (`mapearHallazgos`, hallazgo `margen_negativo`) + `src/lib/calculo-diagnostico.ts` (retención de fugas `usa_margen`) | dominio | 1 | **ya resuelto en HEAD** (commit `e5080e2`, previo a este bloque) — no localizado como pendiente: el hallazgo existe, primero en el array, prioridad alta. Falta verificación visual (no hecha en este bloque, ver `docs/visual/wireframes.md` sección 4) de que se vea DISTINTO al resto — hoy usa el mismo componente `findings` sin color propio (relacionado con E-11) |
| E-11 | Severidad ALTA/MEDIA sin diferencia visual | NORMATIVO | PDF: `src/documents/renderers/pdf/document.tsx:535` (`badgeStyle` único, sin variante por `prioridad`). Web: `src/documents/renderers/web/document-renderer.tsx:197-199` + `src/documents/renderers/web/document-renderer.css:388-404` (`.vdoc-tag--alta`/`.vdoc-tag--media`, colores distintos) | renderer | 2 | confirmado en código para el renderer PDF; **refutado para el renderer web** — el web SÍ tiene colores distintos por prioridad (rojo para alta, amarillo para media). Es una divergencia PDF/web, no un problema en ambos por igual |
| E-12 | Orden de hallazgos no respeta severidad | INFERIDO | `src/lib/propuesta.ts:66-489` (`mapearHallazgos`) — el array se arma en el orden fijo de las ramas del código (medición, mer_bajo, estructura_cuenta, mix_producto, funnel, retención, recompra, mayorista, clips, creativos, ángulo, product_ads, cuotas, comisiones); ningún `.sort()` en `mapearHallazgos` ni en `hallazgosDocumento`/`buildFindings` | dominio/contrato | 2 | confirmado en código |
| E-13 | Daga "†" sin nota al pie — causa pendiente de localizar | NORMATIVO | **LOCALIZADO en este bloque:** `src/documents/renderers/pdf/document.tsx`, caso `"scenarios"` (líneas 582-663) — nunca renderiza `item.assumptions`; comparar con `src/documents/renderers/web/document-renderer.tsx:385-390`, que sí lo hace. El comentario de `src/documents/renderers/pdf/format.ts:22-27` afirma que "el acceso al supuesto es la lista de Supuestos ya impresa debajo de cada tarjeta" — falso para el PDF, verdadero sólo para el web | renderer | 2 | **confirmado en código — causa raíz localizada** (estaba "pendiente de localizar" en la auditoría externa) |
| E-14 | Formato decimal inconsistente (1 vs. 2 decimales) | INFERIDO | PDF: `src/documents/renderers/pdf/format.ts:4-7` (`minimumFractionDigits: 1, maximumFractionDigits: 2`). Web: `src/documents/renderers/web/format.ts:12-15` (`style: "percent", maximumFractionDigits: 2`, **sin** `minimumFractionDigits`) | renderer | 2 | confirmado en código — y es más profundo de lo reportado: el PDF y el web usan configuraciones de `Intl.NumberFormat` DISTINTAS para el mismo formato "percent" (el PDF fuerza mínimo 1 decimal; el web no fuerza ninguno, así que puede mostrar 0 decimales). La inconsistencia no es sólo "a veces 1, a veces 2" dentro de un mismo renderer — es también una divergencia PDF vs. web |
| E-15 | Metadatos crudos concatenados con guiones | NORMATIVO | PDF: `src/documents/renderers/pdf/document.tsx:537-540` (`{item.capa} - confianza {item.confianza}...`, usa el valor crudo del enum). Web: `src/documents/renderers/web/document-renderer.tsx:200` (`{LABELS_CAPA[item.capa]}`, usa la etiqueta traducida) | renderer | 2 | confirmado en código — sólo en el PDF; el renderer web ya traduce `capa` a una etiqueta legible (`LABELS_CAPA`), el PDF no. Divergencia PDF/web, mismo patrón que E-04/E-11/E-14 |
| E-16 | Cero real con confianza "media" genérica | INFERIDO | `src/documents/domain/build-context.ts:131-142` (`publicarNumero`, `confianza: args.confianza ?? "media"`) + `:551-555` (`inversionTotal`, llamada sin `confianza` explícita) | dominio | 3 | confirmado en código — comportamiento general de `publicarNumero`, no un caso aislado: cualquier métrica de `metric-grid` sin `confianza` explícita hereda "media" sin importar la certeza real del valor |

## Inconsistencias de contrato

| ID | Hallazgo (resumido) | Clase | Archivo y línea de la causa | Capa | Bloque que lo corrige | Estado de verificación |
|---|---|---|---|---|---|---|
| C-01 | Perfil A4 no existe como maquetación propia | NORMATIVO | `src/documents/renderers/pdf/document.tsx:99-142` (`PROFILES`, dos perfiles reales) + `src/documents/renderers/web/document-renderer.css:751-775` (`@page`) + `src/documents/renderers/web/document-renderer.tsx:12-15` (sin campo `profile`) | renderer | 1 | **refutado para el renderer PDF, confirmado para el renderer web** — ver desglose completo en `docs/visual/perfiles-pantalla-a4.md`, que también responde V3. Ya queda resuelto documentalmente en este bloque; lo que falta (paginación de tablas, fondos sin sangre, escala tipográfica completa en A4) es trabajo de Bloque 2 |
| C-02 | Un solo eje de estados donde se exigen dos | NORMATIVO | `src/documents/domain/types.ts:11-46` (`Evidencia<T>` existe pero nunca se lee fuera de `build-context.ts`; `ValorPublicable<T>` sólo tiene 3 de los 4 estados de D4) | dominio/contrato | 3 | confirmado en código — desarrollo completo en `docs/visual/contrato-estados.md` |
| C-03 | `servicio` es texto libre, rompe trazabilidad hallazgo → catálogo | NORMATIVO | `src/lib/propuesta.ts:36` (`servicio: string \| null`) + `src/lib/paquetes.ts:121-123` (`serviciosCanonicosDe` SÍ sabe partir strings compuestos, pero sólo se usa para la escalera de paquetes, no para el bloque `services`/`ServicioDocumento`) | contrato | 3 | confirmado en código — ver V2 más abajo |
| C-04 | Se emite propuesta sin selección comercial | NORMATIVO | `src/documents/templates/velocentum-v1/shared.ts:4-30` (`createModel` filtra la sección vacía, no la reemplaza por un aviso) + `src/routes/_authenticated/documentos.$id.$slug.tsx:128-164` (`descargarPdf` sin ninguna condición sobre `comercial`) | contrato + UI | 3 | confirmado en código — ver V4/V5 más abajo |
| C-05 | (reclasificado) | — | — | — | 1 | reclasificado → R-12, ya reflejado en `docs/visual/auditoria-visual-2026-08-23.md` |
| C-06 | (retirado) | — | — | — | 1 | retirado, ya reflejado en `docs/visual/auditoria-visual-2026-08-23.md` |
| C-07 | Sin puente entre cifras del diagnóstico y de la propuesta | INFERIDO | No hay ningún bloque ni texto que relacione `context.hallazgos[].monto` (mensual) con `context.resumenComercial.cifraPrincipal` (acumulado 90 días) en ninguna plantilla — verificado por lectura de `diagnostico.ts`, `propuesta.ts`, `composicion.ts`: ninguna referencia cruzada entre ambos bloques | contrato/plantilla | 2 | confirmado en código (ausencia verificada, no hay dónde más buscarlo) |

## Recomendaciones

| ID | Hallazgo (resumido) | Clase | Archivo y línea relevante | Capa | Bloque que lo corrige | Estado de verificación |
|---|---|---|---|---|---|---|
| R-01 | Contrato de ocupación por página ≥70% (16:9) | RECOMENDACIÓN | No hay ningún mecanismo de medición u ocupación mínima en `document.tsx`/`document-renderer.css` | renderer | 2 | confirmado en código — no existe ningún control de ocupación mínima, consistente con lo reportado |
| R-02 | Balanceo de grilla 4+3, nunca 3+3+1 | RECOMENDACIÓN | `src/documents/renderers/pdf/document.tsx:279` (`cardGrid: {flexWrap: "wrap"}`) — el wrap es automático por ancho de card, sin ningún cálculo de balanceo por cantidad de ítems | renderer | 2 | confirmado en código — 9 métricas fijas (`METRIC_DEFINITIONS`, `blocks.ts:12-26`) en grilla de 3 columnas (`cardWidthGrid: "31.8%"`) da 3+3+3 en pantalla, pero cualquier bloque con 7 ítems (ej. 7 hallazgos) da 3+3+1 sin ningún balanceo |
| R-03 | Escala tipográfica mínima por perfil (legibilidad en videollamada) | RECOMENDACIÓN | `src/documents/renderers/pdf/document.tsx:381-393` (`scenarioMetricLabel: 7.5`, `scenarioMetricValue: 14`) | renderer | 2 | confirmado en código — tamaños fijos, sin variante "modo videollamada"; no hay una segunda escala más grande disponible hoy |
| R-04 | Iconografía lineal violeta en círculo, numeración 01/02/03 | RECOMENDACIÓN | Ver `docs/visual/contrato-estados.md` sección 5 | renderer | 2 | confirmado en código — no existe iconografía de contenido en ningún renderer; la numeración 01/02/03 SÍ existe pero sólo en `FindingsBlock` del renderer WEB (`document-renderer.tsx:192-194`), no en el PDF ni en ningún otro bloque |
| R-05 | Portada con degradado suave, no tres bandas duras | RECOMENDACIÓN | `src/documents/renderers/pdf/document.tsx:213-229` (`coverAccent`/`coverAccentSoft`, dos capas sólidas superpuestas, sin gradiente) | renderer | 2 | confirmado en código |
| R-06 | Un solo tratamiento del wordmark | RECOMENDACIÓN | `src/documents/renderers/web/document-renderer.tsx:116` ("Velocentum", texto plano en portada) vs. `src/documents/renderers/pdf/document.tsx:764` (`"VELOCENTUM"`, mayúsculas, al pie) | renderer | 2 | confirmado en código — dos tratamientos de caja de texto distintos, en dos lugares distintos (portada web en minúscula normal vs. pie de página PDF en mayúsculas) |
| R-07 | Sección de fortalezas para negocios sanos | RECOMENDACIÓN | No existe ningún bloque ni sección "fortalezas" — verificado, cero coincidencias en `src/documents` | contrato/dominio | 3 | confirmado en código (ausencia). Nota: la prioridad ALTA desproporcionada que motivó este hallazgo ya se corrigió en `e5080e2` (ver `docs/visual/wireframes.md` sección 5) — la sección de fortalezas en sí sigue sin existir, es un hallazgo distinto que persiste |
| R-08 | Máximo una divisoria por documento | RECOMENDACIÓN | `src/documents/templates/velocentum-v1/propuesta.ts` — usa `transitionSection` una vez; las "3 de 6 páginas" reportadas en s4-propuesta probablemente cuentan portada + transición + la página vacía de "paquete seleccionado" sin selección comercial (ver C-04) como páginas de bajo contenido, no necesariamente 3 divisorias literales — no verificable con precisión sin regenerar el PDF | renderer/contrato | 2 | no localizado con precisión — la plantilla sólo tiene una `transitionSection`; la sensación de "3 páginas vacías" en la evidencia es más probablemente el efecto combinado de C-04 (página de paquete vacía/ausente) y baja ocupación (R-01), no divisorias literales de más |
| R-09 | Alinear título y contenido | RECOMENDACIÓN | `src/documents/templates/velocentum-v1/diagnostico.ts:71-76` (`title: "Funnel, retención y hallazgos priorizados"`, `blocks: [findings.block]` — sólo hallazgos, nunca datos de funnel ni de retención con estructura propia) | contrato/plantilla | 3 | confirmado en código — el título promete "funnel y retención" pero el bloque sólo trae hallazgos (que pueden o no incluir hallazgos de funnel/retención según los datos); no hay un desglose de funnel ni de retención con su propio componente (mismo gap ya documentado en `docs/plan-maestro-fases.md`, fase 11, "desglose por canal/producto/funnel pendiente") |
| R-10 | Comparación entre canales en diagnósticos multicanal | RECOMENDACIÓN | `src/documents/domain/types.ts:224-234` (`MetricasActualesDocumento` — `merTienda`/`merMarketplace` son dos campos planos, sin ningún componente de comparación lado a lado) | contrato/renderer | 2 | confirmado en código — ambos MER se muestran como dos tarjetas de `metric-grid` sin relación visual explícita entre sí |
| R-11 | Rediseñar escenarios en 16:9 (peor aprovechamiento que A4) | RECOMENDACIÓN | `src/documents/renderers/pdf/document.tsx:99-118` (`pantalla.cardWidthGrid: "31.8%"`, 3 columnas, cards angostas) | renderer | 2 | confirmado en código como plausible — cards más angostas en pantalla (3 columnas) que en impresión (2 columnas, `"48.8%"`) para el mismo contenido (misma cantidad de datos por escenario) es consistente con la observación, aunque "aprovecha peor el espacio" es un juicio visual no medible desde el código solo |
| R-12 | Presentación de la tabla mensual (ex C-05) | RECOMENDACIÓN | Contrato ya aprobado (D2); implementación actual en `src/documents/renderers/pdf/document.tsx:623-648` y `src/documents/renderers/web/document-renderer.tsx:319-371` | renderer | 2 | confirmado — la tabla existe y respeta D2 a nivel de datos (tres magnitudes en columnas separadas), pero comparte la causa de overflow de E-01; el layout definitivo queda para Bloque 2, tal como indica la propia recomendación |

## Verificaciones específicas (V1 a V5)

| # | Pregunta | Veredicto | Evidencia |
|---|---|---|---|
| V1 | Palancas en lista única, sin etiqueta de magnitud/período, valor = ritmo mensual día 90 | **CONFIRMADO** | `src/lib/escenarios-90d.ts:156,202,254`; `src/documents/domain/escenarios-90d.ts:120-124`; `src/documents/templates/velocentum-v1/blocks.ts:199-207`; PDF `document.tsx:650-654`; web `document-renderer.tsx:372-384`. `lever.type` se transporta en el modelo pero sólo se usa como `key` de React en ambos renderers, nunca como texto visible |
| V2 | `servicio` es texto libre, admite combinaciones fuera del catálogo de seis | **CONFIRMADO** | `src/lib/propuesta.ts:36` (`servicio: string \| null`); ejemplo real hardcodeado en `propuesta.ts:216`; `src/documents/domain/build-context.ts:268-281` deduplica strings crudos sin pasarlos por `serviciosCanonicosDe` (`src/lib/paquetes.ts:121-123`), que sí existe pero se usa sólo para la escalera de paquetes |
| V3 | No existe maquetación A4 propia separada de la 16:9 | **REFUTADO para el renderer PDF, CONFIRMADO para el renderer web** | Ver `docs/visual/perfiles-pantalla-a4.md`, secciones 1 y 2, con el matiz de que el PDF comparte un único árbol de componentes parametrizado, no dos jerarquías separadas |
| V4 | El bloque de oferta comercial y `SeleccionComercial` existen en el modelo; la propuesta se emite igual sin selección confirmada | **CONFIRMADO** | Tipo `SeleccionComercial`/`NivelComercial` en `src/documents/domain/types.ts`; `buildCommercialOffer` (`blocks.ts:283-319`) devuelve `{block: null}` sin selección, y esa sección simplemente desaparece del modelo (`shared.ts:4-30`, `createModel` filtra secciones vacías) — el resto del documento se genera igual, sin ningún bloqueo |
| V5 | Existe un punto único donde se decide emitir/exportar un documento | **CONFIRMADO (existe el punto, no la restricción)** | `src/routes/_authenticated/documentos.$id.$slug.tsx:128-143` (`descargarPdf`) → `src/documents/renderers/pdf/export-client.ts:16-31` (`downloadDocumentModelPdf`) es el único camino de descarga en todo el repositorio (verificado: `downloadDocumentModelPdf`/`renderDocumentModelToBlob` no se llaman desde ningún otro archivo). Hoy no tiene ninguna condición sobre `model.kind`/`context.comercial` — es el lugar correcto para alojar la restricción de D1, pero la restricción en sí no existe todavía |
