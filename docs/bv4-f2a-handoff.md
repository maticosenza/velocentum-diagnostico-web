# BV4 · F2a Panel de selección comercial — handoff

1. **F2a completa, etapas 0 a 6, más tres rondas de corrección post-auditoría: las dos del contrato y una tercera autorizada por excepción explícita de Matías —el reparto 30/60/90 es lógica de negocio, no presentación, y diferirlo a F3b habría dejado que una fase de arte rediseñara el render sobre un reparto incorrecto. Commit candidato LOCAL, sin push.** Rama `feat/bv4-rebranding` sobre `043ba08`. `main` intacto, sin migraciones de esquema, `MOTOR_DOCUMENTAL_ACTIVO` en `"v1"` y `TEMA_DOCUMENTAL_ACTIVO` en `"velocentum-light-v1"` en todos los commits.
2. QA: `npm test` **82 archivos / 1128 pruebas + 1 todo**, verdes (base: 72 / 903; **+10 archivos, +225 pruebas**, cero regresiones) · `tsc --noEmit` exit 0 · `vite build` exit 0 con **exactamente los dos mismos warnings que el base `043ba08`** (chunk >500 kB en `paginacion`/`marca`, e `inlineDynamicImports`), verificados corriendo el build del base en un worktree aparte. **Determinismo probado por doble corrida, dentro y entre procesos:** los cuatro PDFs del gate (Snake Store y Titan Web B1 × pantalla y A4) dan **SHA-256 idéntico** al renderizarse dos veces en la misma corrida y también en dos corridas separadas; la **matriz de 54 regenerada** da los **54 hashes idénticos** entre dos corridas independientes. El navegador y el pipeline llaman a la misma `renderPdfV2ConDosPasadas`, y por eso los hashes del gate pueden coincidir.
3. **Diagnóstico de la etapa 0 (`docs/bv4-f2a-diagnostico.md`):** el panel de la Fase 13 existe y funciona, pero resuelve otro problema — confirma una *escalera de niveles* con un precio por nivel; F2a pide *diez líneas facturables* con precio unitario, cantidad y recurrencia. Los rieles se reutilizaron enteros; el modelo comercial es construcción nueva. 9 de los 10 puntos de Q1–Q10 estaban en cero.
4. **F-2 cumplida y probada a tres alturas.** El sobre v2 vive **dentro** de la clave `paquetes`; la raíz de la columna sigue siendo `{ propuesta, paquetes }`. La escalera legada se preserva en `SobreComercialV2.legado` y la cadena v1 la lee por un punto único: el `DocumentContextV1` es idéntico salvo el campo nuevo, y el **modelo v1 armado es idéntico —serialización incluida— para las cuatro plantillas**. Una quinta prueba de control verifica que si el legado se perdiera, el modelo v1 **sí** cambiaría.
5. **Q1–Q10 cerradas en código, no en comentarios:** el total combinado **no es representable** (tipo sin campo, más `@ts-expect-error` que rompería el typecheck); los totales **no se persisten**, se calculan; precio unitario y precio de línea son **excluyentes por tipo**; una línea seleccionada sin precio **no vale cero**, sale aparte; y ninguna combinación de hallazgos puede sugerir influencer marketing ni desarrollo custom (barrido exhaustivo de los 256 subconjuntos).
6. **Textos de los diez servicios: verbatim de verdad.** La prueba **lee `docs/funcional/f2a-textos-servicios.md`**, lo parsea y lo confronta contra el módulo carácter por carácter. Reescribir, resumir o "mejorar" un texto rompe la suite; cambiar el documento también, y ahí la decisión vuelve a ser humana. Semana 0: tres invariantes, ninguno pasable.
7. **Decisiones derivadas, señaladas para tu revisión:** (a) `SeleccionComercialV2` lleva `nivel` — sin él, "el alcance sigue al nivel elegido", CRO sólo en ESCALA y las precargas por nivel no son representables; (b) `cliente.moneda` **no** se tocó y sigue en `"ARS"` (es la moneda de operación del cliente; la de la propuesta vive en `comercialV2.moneda`, porque mezclarlas habría rotulado en dólares la facturación y el margen); (c) los agregados no llevan precio y no entran en ningún subtotal; (d) el panel v1 se conserva visible bajo "Paquetes propuestos (escalera v1)" como ancla de reversión.
8. **Ronda 3 (2026-09-01), excepción autorizada: el reparto 30/60/90.** El defecto: en los cuatro PDFs, Titan Web tenía **una sola etapa** —"DÍAS 61-90"— y Snake Store **no tenía la 1-30**. Causa: la regla preexistente mandaba a la etapa 90 toda línea sin hallazgo que la justificara, y con el modelo v2 las líneas vienen de la selección comercial, no del mapeo de hallazgos. Ahora **cada línea cotizada reparte sus entregables verbatim entre las tres etapas** según la tabla confirmada por Matías (R1 infraestructura antes que pauta: diseño web completo en 1-30; R2 contenido en los tres meses; R3 la pauta progresa activar → optimizar → escalar). El reparto **no guarda texto: guarda índices** a los entregables de `textos-servicios-v2.ts`, así que por construcción ninguna frase del plan puede estar fuera de `docs/funcional/f2a-textos-servicios.md` — la prueba lee el markdown fuente y lo confronta. Los hallazgos **siguen** alimentando el plan: la regla se suma, no los reemplaza. Sólo el camino v2; `context.roadmap` no se tocó y su prueba de regresión lo fija. Gate verificado **sobre el texto extraído de los cuatro PDFs**, no sobre el modelo. Lo que la ronda no corrigió está registrado con ID en `docs/bv4-f2a-hallazgos-diferidos.md`: H-1 y H-5 son presentación y van a F3b (H-5 confirmado por vos); H-2 pide un cambio de modelo comercial; H-3 es una nota. **H-4 —el único límite que tuvo el reparto— quedó resuelto en la auditoría del handoff:** Google Ads no tenía viñeta de escala en el texto verbatim, así que la ronda **se frenó** y entregó la etapa 61-90 sin ese renglón, con una prueba que fijaba el hueco. Vos resolviste el fondo —Google Ads sí escala en el mes 3— y aportaste la sexta viñeta al documento fuente; el reparto la toma y la prueba cambió de sentido: ahora exige el renglón **y** que la frase esté en el markdown. R3 se cumple en las tres líneas de pauta. **Supuesto:** el reparto de desarrollo web custom vale para el caso de varios meses; el modelo no tiene campo de duración y no se inventó uno.
9. **Correcciones post-auditoría, rondas 1 y 2.** *Ronda 1:* los PDFs mostraban dos secciones comerciales contradictorias —"Paquete seleccionado · Selección comercial pendiente" y, una página después, la propuesta cotizada—; con selección v2 confirmada, la escalera v1 dejó de imprimirse. *Ronda 2, tres cambios:* (a) la regla se extiende al **caso espejo** — con selección v2 presente, **confirmada o no**, la v2 es la única voz comercial; (b) **el candado cambia**: una v2 pendiente bloquea la exportación **aunque exista escalera legada confirmada**, porque entrar por la escalera vieja permitía exportar una propuesta con precios sin configuración fiscal confirmada y eso vaciaba Q9; (c) el **roadmap 30/60/90** se arma desde la selección v2, para que el plan no describa un paquete distinto del cotizado. Todo en `roadmapV2`, un campo aparte: `context.roadmap` no se tocó porque es el que renderizan las plantillas v1, y pisarlo habría cambiado la salida v1. El reparto 30/60/90 se extrajo a una función compartida **sin cambiar una sola regla**, y las pruebas de DHB-3 que ya existían lo fijan. Un detalle que apareció recién al mirar los PDFs regenerados: Q1 hace que un mismo hallazgo justifique tres líneas de contenido, así que el plan imprimía el mismo renglón tres veces; se descartan los repetidos **literales** dentro de cada etapa, **sólo en el camino v2** —el v1 no deduplica, para que su salida siga siendo idéntica.
10. Artefactos generados desde un **worktree limpio** del commit candidato: los nueve documentos (los siete de siempre más el prompt de la ronda 3 y los hallazgos diferidos), los logs de QA y los cuatro PDFs con su `sha256.txt`. El hash del candidato y el SHA-256 del ZIP están en el `LEEME.txt` de adentro del ZIP y en el mensaje de entrega — no acá, porque el ZIP se arma DESPUÉS de commitear este archivo y una copia con el hash escrito a mano quedaría desactualizada dentro del propio ZIP. `artefactos/` está en `.gitignore` por diseño: los ZIP no se commitean, tampoco el de F1. **Detención completa, sin push.**
