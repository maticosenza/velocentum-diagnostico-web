# BV4 · F2a Panel de selección comercial — handoff

1. **F2a completa, etapas 0 a 6, commit candidato LOCAL sin push.** Rama `feat/bv4-rebranding` sobre `043ba08`. `main` intacto, sin migraciones de esquema, `MOTOR_DOCUMENTAL_ACTIVO` en `"v1"` y `TEMA_DOCUMENTAL_ACTIVO` en `"velocentum-light-v1"` en todos los commits.
2. QA: `npm test` **81 archivos / 1083 pruebas + 1 todo**, verdes (base: 72 / 903; **+9 archivos, +180 pruebas**, cero regresiones) · `tsc --noEmit` exit 0 · `vite build` exit 0 con **exactamente los dos mismos warnings que el base `043ba08`** (chunk >500 kB en `paginacion`/`marca`, e `inlineDynamicImports`), verificados corriendo el build del base en un worktree aparte.
3. **Determinismo probado por doble corrida, dentro y entre procesos:** los cuatro PDFs del gate (Snake Store y Titan Web B1 × pantalla y A4) dan **SHA-256 idéntico** al renderizarse dos veces en la misma corrida y también en dos corridas separadas; la **matriz de 54 regenerada** da los **54 hashes idénticos** entre dos corridas independientes. El navegador y el pipeline llaman a la misma `renderPdfV2ConDosPasadas`, y por eso los hashes del gate pueden coincidir.
4. **Diagnóstico de la etapa 0 (`docs/bv4-f2a-diagnostico.md`):** el panel de la Fase 13 existe y funciona, pero resuelve otro problema — confirma una *escalera de niveles* con un precio por nivel; F2a pide *diez líneas facturables* con precio unitario, cantidad y recurrencia. Los rieles se reutilizaron enteros; el modelo comercial es construcción nueva. 9 de los 10 puntos de Q1–Q10 estaban en cero.
5. **F-2 cumplida y probada a tres alturas.** El sobre v2 vive **dentro** de la clave `paquetes`; la raíz de la columna sigue siendo `{ propuesta, paquetes }`. La escalera legada se preserva en `SobreComercialV2.legado` y la cadena v1 la lee por un punto único: el `DocumentContextV1` es idéntico salvo el campo nuevo, y el **modelo v1 armado es idéntico —serialización incluida— para las cuatro plantillas**. Una quinta prueba de control verifica que si el legado se perdiera, el modelo v1 **sí** cambiaría.
6. **Q1–Q10 cerradas en código, no en comentarios:** el total combinado **no es representable** (tipo sin campo, más `@ts-expect-error` que rompería el typecheck); los totales **no se persisten**, se calculan; precio unitario y precio de línea son **excluyentes por tipo**; una línea seleccionada sin precio **no vale cero**, sale aparte; y ninguna combinación de hallazgos puede sugerir influencer marketing ni desarrollo custom (barrido exhaustivo de los 256 subconjuntos).
7. **Textos de los diez servicios: verbatim de verdad.** La prueba **lee `docs/funcional/f2a-textos-servicios.md`**, lo parsea y lo confronta contra el módulo carácter por carácter. Reescribir, resumir o "mejorar" un texto rompe la suite; cambiar el documento también, y ahí la decisión vuelve a ser humana. Semana 0: tres invariantes, ninguno pasable.
8. **Decisiones derivadas, señaladas para tu revisión:** (a) `SeleccionComercialV2` lleva `nivel` — sin él, "el alcance sigue al nivel elegido", CRO sólo en ESCALA y las precargas por nivel no son representables; (b) `cliente.moneda` **no** se tocó y sigue en `"ARS"` (es la moneda de operación del cliente; la de la propuesta vive en `comercialV2.moneda`, porque mezclarlas habría rotulado en dólares la facturación y el margen); (c) los agregados no llevan precio y no entran en ningún subtotal; (d) el panel v1 se conserva visible bajo "Paquetes propuestos (escalera v1)" como ancla de reversión.
9. **Pendiente humano:** el flujo en navegador lo ejecutás vos. Los pasos exactos, incluida la conmutación temporal de `motor-activo.ts` a `"v2"` y su reversión (F-3), están en `docs/bv4-f2a-gate-navegador.md`. Sin esa conmutación no se ve el bloque nuevo: vive en la cadena v2.
10. Commit candidato **`c98023995a69be9b4a9f032bb4d78f6bca9eb2ba`**. Artefactos en `artefactos/bv4-f2a-artefactos-c980239.zip` (SHA-256 `59d2994f8c3c3ad5e0f7c54c7dbdd0729363eac672187b6a87f55b2a77b697c1`), generado desde un **worktree limpio** de ese commit: los siete documentos, once logs de QA y los cuatro PDFs con su `sha256.txt`. **Detención completa, sin push.** El push llega recién con tu autorización expresa después de la auditoría externa y del gate en navegador.
