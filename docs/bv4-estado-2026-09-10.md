# BV4 · Estado del trabajo al 2026-09-10

Documento para retomar desde cero. Rama `feat/bv4-rebranding`, HEAD `e22be02`,
remoto `origin/feat/bv4-rebranding`. Tres commits locales sin pushear (abajo).
Árbol limpio al momento de escribir esto.

## 1 · Las tres auditorías previstas

| Auditoría | Estado | Registro | Commit |
|---|---|---|---|
| Formulario de carga | hecha | H-18 a H-27 en `docs/bv4-hallazgos-diferidos.md` | `89fda9c` |
| Motor de cálculo | hecha | H-28 a H-37 en `docs/bv4-hallazgos-diferidos.md` | `e22be02` |
| Salidas (pantalla de detalle, proyección y propuesta) | **pendiente** | — | — |

**Próximo ID libre: H-38.**

**Ninguno de los 20 hallazgos (H-18 a H-37) está corregido.** Todos figuran
como abiertos en el registro. Los cuatro del motor que tienen diseño de
arreglo (H-28, H-29, H-30 y H-34) lo tienen en
`docs/bv4-motor-arreglos-propuestos.md`, como propuesta sin aprobar y sin
aplicar.

## 2 · Commits locales sin pushear

Verificados con `git log` y `git show --stat` antes de describirlos. Son
exactamente los tres que `origin/feat/bv4-rebranding..HEAD` devuelve.

- **`ac3b3f2` (2026-09-05) · fix(diagnostico): el numero principal distingue
  cero real de dato pendiente.** Toca un solo archivo:
  `src/routes/_authenticated/diagnosticos.$id.tsx` (+41). `NumeroPrincipal`
  gana un tercer guard: con `oportunidad_total` en 0 y al menos una fuga con
  `calculable: false`, no muestra el rango "$0 a $0" sino el dato que falta,
  nombrado con `ETIQUETAS_CAMPO`. Si el 0 sale de fugas todas calculables, es
  un cero real y se muestra como antes. Sin cambios en el motor ni en los
  fixtures. Ver la nota de la sección 4.
- **`89fda9c` (2026-09-10) · docs: registra H-18 a H-27, auditoría del
  formulario de carga del 2026-09-10.** Toca solo
  `docs/bv4-hallazgos-diferidos.md` (+253/−14): diez hallazgos del
  formulario (`diagnosticos.nuevo.tsx`, `campos-formulario.tsx`,
  `bloque-canales.tsx`, `diagnostico-form.ts`), todos abiertos, con
  evidencia archivo:línea; el párrafo de estado pasa al 2026-09-10.
- **`e22be02` (2026-09-10) · docs: registra la auditoría del motor (H-28 a
  H-37) y el diseño de los cuatro arreglos.** Toca
  `docs/bv4-hallazgos-diferidos.md` (+72/−8) y crea
  `docs/bv4-motor-arreglos-propuestos.md` (+100): diez hallazgos del motor
  (`calculo-diagnostico`, `contradiccion`, `mayorista`, `funnel`,
  `canales`), todos abiertos, con evidencia y entrada que los dispara; el
  diseño de los arreglos 1, 2, 3 y 7 con su impacto en fixtures y tests; y,
  sin ID, los tres puntos que la auditoría dejó afuera y las tres sospechas
  sin verificar. Nada en `src/`.

## 3 · Decisiones pendientes que no son de código

**Cuáles de los cuatro arreglos del motor entran y en qué orden.** El orden
recomendado en `docs/bv4-motor-arreglos-propuestos.md` es **3, 7, 2, 1**: los
tres primeros no mueven ningún fixture; el 1 es el de mayor impacto comercial
pero es el único que cambia números ya publicados en docs y un assert
(`calculo-diagnostico.test.ts:1221`, compras 180 → 108) y desactualiza los
montos del escenario demo 1 en `docs/loop-nocturno-2026-08-22-escenarios.md`.
Con los cuatro, `generar-propuestas-f2a.test.ts` queda verde.

**Decisiones de producto que cada arreglo requiere**, según ese documento:

- **Arreglo 1 (H-28, conversión y funnel sobre facturación de tienda):**
  ninguna decisión bloqueante. Conviene decidir si `pedidos_mensuales` se
  mantiene como cifra del negocio: la vista muestra "Pedidos mensuales
  estimados" y "Compras estimadas" en filas distintas y, tras el arreglo, van
  a diferir en negocios mixtos. Recomendación: mantenerlo y etiquetar.
- **Arreglo 2 (H-29, inversión publicitaria parcial):** dos decisiones.
  (a) Si se infiere "no invierte en ML" de "ML no participa del mix" (canal
  ausente con los demás al 100%), o si se exige `no_aplica` explícito
  agregando `canal_ml_no_aplica: true` a los escenarios demo 2 a 5. La
  segunda es más honesta; cuesta tocar cuatro fixtures. (b) Qué significa
  Google vacío con Meta cargado: no hay campo que distinga "no pauta en
  Google" de "no lo relevé"; hoy se suma como cero; retener pide un campo o
  una convención en el formulario. El caso Titan Web B1 (1.800.000 correcto
  con tienda en `no_aplica`) queda intacto con la regla propuesta.
- **Arreglo 3 (H-30, resultado después de publicidad):** ninguna.
- **Arreglo 7 (H-34, porcentajes de producto que suman más de 100):** si el
  formulario debe impedirlo (hoy no valida la suma). Si lo bloquea, el
  arreglo del motor es defensivo. Si no, decidir qué ve el usuario: "120%
  del catálogo" o la restricción "mix de productos inconsistente" con el
  porcentaje escondido.

**Además:** hacer o no la auditoría de salidas antes de aplicar arreglos, y si
las seis notas sin ID del final de `docs/bv4-motor-arreglos-propuestos.md`
(tres fuera del reporte, tres sospechas) merecen ID propio.

## 4 · Nota sobre el arreglo del "$0 a $0" (`ac3b3f2`)

El arreglo tocó **sólo la pantalla de detalle**
(`src/routes/_authenticated/diagnosticos.$id.tsx`). El listado de
diagnósticos sigue mostrando "$ 0" para el mismo caso:
`src/routes/_authenticated/index.tsx:188-190` imprime
`formatARS(f.oportunidad_total)` siempre que el valor sea un número, y un
`oportunidad_total` en 0 por fugas no calculables es un número. Verificado en
esa línea al escribir esto.

Registrado el 2026-09-11 como **H-49** en `docs/bv4-hallazgos-diferidos.md`,
abierto. El listado no puede distinguir el cero real del cero por ignorancia
porque su query no trae `fugas` y la tabla no tiene una columna que lo
marque. Se evaluaron tres opciones y se descartaron (traer el jsonb por fila,
o esconder también el cero real); la correcta es una columna derivada al
guardar, que toca la base y espera decisión explícita. El listado queda como
está.

## 5 · Dónde está cada cosa

- Registro de hallazgos con ID: `docs/bv4-hallazgos-diferidos.md` (H-6 en
  adelante) y `docs/bv4-f2a-hallazgos-diferidos.md` (H-1 a H-5).
- Diseño de los arreglos del motor: `docs/bv4-motor-arreglos-propuestos.md`.
- Gate de F2a y su cierre: `docs/bv4-f2a-gate-navegador.md`,
  `docs/bv4-gate-contenido-handoff.md`.
- Fixtures compartidos de regresión: `src/lib/fixtures-casos.ts` (Snake
  Store, Titan Web B1) y `src/lib/fixtures-escenarios-demo.ts` (seis
  escenarios).
